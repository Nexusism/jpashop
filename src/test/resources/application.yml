spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
# show_sql: true
#        format_sql: true

# 스프링부트에서 별도의 설정이 없으면 H2 memory모드로 자동으로 설정함
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
# org.hibernate.type: trace